apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-nginx-config
data:
  default.conf: |
    server {
      listen 80;
      cation / {
        loroot /usr/share/nginx/html;
        index index.html;
      }
      location /stub_status {
        stub_status;
      }
    }
